import React, { useCallback, useMemo, useState } from "react";
import { DendronProps } from "../types";

export default function DendronCalendarPanel({ name }: DendronProps) {
  return <h1>DendronCalenderPanel</h1>;
}